[project]
name = "janus-md"
version = "0.1.0"
description = "Dual-state static site generator — HTML for humans, Markdown for AI"
requires-python = ">=3.11"

dependencies = [
    "markdown>=3.7",
    "pygments>=2.18",
    "pyyaml>=6.0",
    "jinja2>=3.1",
]
